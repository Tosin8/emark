import 'package:flutter/material.dart';

class Favs extends StatelessWidget {
  const Favs({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold( body: Center(child: Text('This is the Favs page'),));
  }
}